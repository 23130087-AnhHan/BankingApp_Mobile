server:
  port: 8082

spring:
  application:
    name: user-service
    bad_request: 400
    conflict: 409
    success: 200
    not_found: 404

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB_NAME:user_service}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    default-encoding: UTF-8
    username: phannguyenminhthang456@gmail.com
    password: bxuhrxevwxbiliew
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


app:
  config:
    keycloak:
      server-url: ${KEYCLOAK_URL:http://localhost:8571}
      realm: ${KEYCLOAK_REALM:banking-service}
      client-id: ${KEYCLOAK_CLIENT_ID:banking-service-api-client}
      client-secret: ${KEYCLOAK_CLIENT_SECRET}
