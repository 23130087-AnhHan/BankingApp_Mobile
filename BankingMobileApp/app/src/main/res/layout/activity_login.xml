<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg"
    android:clipToPadding="false"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="32dp"
        android:paddingEnd="20dp"
        android:paddingBottom="32dp">

        <TextView style="@style/MutedText" android:text="Truy cập tài khoản" />

        <TextView
            style="@style/PageTitle"
            android:layout_marginTop="2dp"
            android:text="Đăng nhập" />

        <TextView
            style="@style/MutedText"
            android:layout_marginTop="8dp"
            android:text="Đăng nhập bằng email và mật khẩu đã đăng ký. Phiên truy cập được xác thực an toàn qua hệ thống ngân hàng." />

        <LinearLayout
            style="@style/Panel"
            android:orientation="vertical">

            <TextView
                style="@style/SectionTitle"
                android:text="Thông tin truy cập" />

            <TextView
                style="@style/FieldLabel"
                android:text="Email" />

            <EditText
                android:id="@+id/userIdInput"
                style="@style/Input"
                android:hint="tenban@email.com"
                android:inputType="textEmailAddress" />

            <TextView
                style="@style/FieldLabel"
                android:text="Mật khẩu" />

            <EditText
                android:id="@+id/passwordInput"
                style="@style/Input"
                android:hint="Nhập mật khẩu"
                android:inputType="textPassword" />

            <Button
                android:id="@+id/loginButton"
                style="@style/PrimaryButton"
                android:text="Đăng nhập" />
        </LinearLayout>

        <Button
            android:id="@+id/registerButton"
            style="@style/SecondaryButton"
            android:text="Chưa có hồ sơ? Đăng ký" />

        <Button
            android:id="@+id/forgotPasswordButton"
            style="@style/SecondaryButton"
            android:text="Quên mật khẩu?" />

        <TextView
            android:id="@+id/resultText"
            style="@style/ResultBox"
            android:text="Nhập email và mật khẩu để tiếp tục." />
    </LinearLayout>
</ScrollView>
