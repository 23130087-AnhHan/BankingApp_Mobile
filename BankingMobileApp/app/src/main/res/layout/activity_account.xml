<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg"
    android:clipToPadding="false"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="24dp"
        android:paddingEnd="20dp"
        android:paddingBottom="32dp">

        <TextView style="@style/MutedText" android:text="Dịch vụ tài khoản" />
        <TextView style="@style/PageTitle" android:layout_marginTop="2dp" android:text="Quản lý tài khoản" />

        <LinearLayout style="@style/Panel" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Mở tài khoản mới" />
            <TextView style="@style/MutedText" android:layout_marginBottom="16dp" android:text="Tạo tài khoản thanh toán gắn với mã khách hàng." />

            <TextView style="@style/FieldLabel" android:text="Mã khách hàng" />
            <EditText android:id="@+id/userIdInput" style="@style/Input" android:hint="Nhập User ID" android:inputType="number" />

            <TextView style="@style/FieldLabel" android:text="Loại tài khoản" />
            <EditText android:id="@+id/accountTypeInput" style="@style/Input" android:hint="SAVINGS_ACCOUNT" />

            <Button android:id="@+id/createAccountButton" style="@style/PrimaryButton" android:text="Mở tài khoản tiết kiệm" />
            <Button android:id="@+id/findAccountButton" style="@style/SecondaryButton" android:text="Tìm theo mã khách hàng" />
        </LinearLayout>

        <LinearLayout style="@style/Panel" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Tra cứu và kích hoạt" />
            <TextView style="@style/MutedText" android:layout_marginBottom="16dp" android:text="Nhập số tài khoản để xem số dư hoặc kích hoạt sử dụng." />

            <TextView style="@style/FieldLabel" android:text="Số tài khoản" />
            <EditText android:id="@+id/accountNumberInput" style="@style/Input" android:hint="Nhập số tài khoản" android:inputType="number" />

            <Button android:id="@+id/activateButton" style="@style/PrimaryButton" android:text="Kích hoạt tài khoản" />
            <Button android:id="@+id/balanceButton" style="@style/SecondaryButton" android:text="Xem số dư" />
        </LinearLayout>

        <TextView android:id="@+id/resultText" style="@style/ResultBox" android:text="Thông tin tài khoản sẽ hiển thị tại đây." />
    </LinearLayout>
</ScrollView>
