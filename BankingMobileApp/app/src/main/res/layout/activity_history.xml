<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg"
    android:clipToPadding="false"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout style="@style/ScreenRoot">

        <TextView
            style="@style/MutedText"
            android:text="Biến động tài khoản" />

        <TextView
            style="@style/PageTitle"
            android:layout_marginTop="2dp"
            android:text="Lịch sử giao dịch" />

        <LinearLayout
            style="@style/Panel"
            android:orientation="vertical">

            <TextView
                style="@style/SectionTitle"
                android:text="Tài khoản hiện tại" />

            <TextView
                android:id="@+id/currentAccountText"
                style="@style/ResultBox"
                android:layout_marginTop="0dp"
                android:text="Đang kiểm tra tài khoản..." />

            <Button
                android:id="@+id/loadHistoryButton"
                style="@style/PrimaryButton"
                android:text="Làm mới lịch sử" />
        </LinearLayout>

        <TextView
            android:id="@+id/historyText"
            style="@style/ResultBox"
            android:text="Các giao dịch sẽ hiển thị tại đây theo thứ tự mới nhất." />
    </LinearLayout>
</ScrollView>
