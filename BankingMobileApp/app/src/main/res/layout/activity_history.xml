<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <TextView style="@style/MutedText" android:text="Account activity" />
        <TextView style="@style/PageTitle" android:text="Transactions" />

        <LinearLayout style="@style/Panel" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Search history" />

            <EditText android:id="@+id/accountNumberInput" style="@style/Input" android:hint="Account number" />
            <Button android:id="@+id/loadHistoryButton" style="@style/PrimaryButton" android:text="Load transactions" />
        </LinearLayout>

        <TextView android:id="@+id/historyText" style="@style/ResultBox" android:text="Transactions will appear here." />
    </LinearLayout>
</ScrollView>
