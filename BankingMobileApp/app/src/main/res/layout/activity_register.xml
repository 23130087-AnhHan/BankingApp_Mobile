<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <TextView style="@style/MutedText" android:text="Customer onboarding" />
        <TextView style="@style/PageTitle" android:text="Create profile" />

        <LinearLayout style="@style/Panel" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Personal details" />

            <EditText android:id="@+id/firstNameInput" style="@style/Input" android:hint="First name" />
            <EditText android:id="@+id/lastNameInput" style="@style/Input" android:hint="Last name" />
            <EditText android:id="@+id/phoneInput" style="@style/Input" android:hint="Phone number" />
            <EditText android:id="@+id/emailInput" style="@style/Input" android:hint="Email address" android:inputType="textEmailAddress" />
            <EditText android:id="@+id/passwordInput" style="@style/Input" android:hint="Password" android:inputType="textPassword" />

            <Button android:id="@+id/registerButton" style="@style/PrimaryButton" android:text="Create customer" />
        </LinearLayout>

        <TextView android:id="@+id/resultText" style="@style/ResultBox" android:text="Registration result will appear here." />
    </LinearLayout>
</ScrollView>
